# 🚀 SkillPilot — карьерный помощник (Ollama локально)

**SkillPilot** помогает быстро адаптировать резюме под вакансию: скоринг соответствия (Job‑Fit), STAR‑буллеты, сопроводительное, план на 7 дней, граф навыков и отчёты (Executive Summary).  
Работает офлайн: LLM и эмбеддинги через локальный **Ollama**.

---

## Системные требования

- **OS:** Linux / macOS (Windows WSL2 тоже ок)
- **Docker + Docker Compose** (рекомендуется для прод/демо)
- **Ollama** (в докере или нативно)
- **RAM:** 8–16 GB (зависит от модели)  
- **GPU (опционально):** ускорит генерацию (CUDA/ROCm)

---

## Переменные окружения (`.env`)

> Вариант, когда **модель и приложение запускаются раздельно**: Ollama — в отдельном контейнере/процессе; App коннектится к нему по `OLLAMA_HOST`.

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
# ==== LLM только локальная Ollama ====
LLM_BACKEND=ollama

# Если Ollama поднимается через docker-compose (сервис "ollama"):
OLLAMA_HOST=http://ollama:11434
OLLAMA_MODEL=mistral

# ==== Embeddings (локально, кэшируются в HF_HOME) ====
EMB_MODEL=all-MiniLM-L6-v2

# ==== Сервер приложения ====
HOST=0.0.0.0
PORT=7860
```

> `OLLAMA_MODEL` — имя модели для генерации (пример: `mistral`, `llama3.1`, `qwen2.5`, и т.д.).  
> Убедитесь, что модель **загружена в Ollama**: `ollama pull mistral` (или через контейнер).

---

## Быстрый старт: Docker Compose (модель и приложение отдельно)

Компоновка:  
- Сервис **`ollama`** — поднимает локальный сервер моделей на порту `11434`  
- Сервис **`app`** — сама веб‑UI на `http://localhost:7860` и подключается к `OLLAMA_HOST=http://ollama:11434`

**1) Установка Docker/Compose** (если еще не стоят):

```bash
# Ubuntu пример
curl -fsSL https://get.docker.com | sh
sudo usermod -aG docker $USER
# Перелогиньтесь в сессию или: newgrp docker
```

**2) Файл `docker-compose.yml`:**

```yaml
Docker-compose не менять
```

**3) Старт стека:**
```bash
docker compose up -d --build
```

**4) Загрузка модели в Ollama (один раз):**
```bash
Модель автоматически скачается за 10 минут. Потом можно проверить ее готовность - 
docker exec -it ollama ollama list
```

**5) Открыть UI:**  
`http://localhost:7860`

---

## Локальный запуск без Docker (если Ollama установлен нативно)

**1) Установите Ollama**  
- macOS: `brew install ollama && ollama serve &`
- Linux: `curl -fsSL https://ollama.com/install.sh | sh && ollama serve &`  
(Убедитесь, что `ollama` слушает на `http://127.0.0.1:11434`)

**2) Скачайте модель:**
```bash
ollama pull mistral
```

**3) Создайте `.env` как выше, но с локальным хостом:**
```env
LLM_BACKEND=ollama
OLLAMA_HOST=http://127.0.0.1:11434
OLLAMA_MODEL=mistral
EMB_MODEL=all-MiniLM-L6-v2
HOST=0.0.0.0
PORT=7860
```

**4) Установите зависимости и запустите:**
```bash
# в корне проекта
python3 -m venv .venv
source .venv/bin/activate
pip install -U pip
pip install -r requirements.txt

python -m skillpilot.ui.app
# или с uvicorn/gradio runner, если предусмотрено в проекте
```

Откройте: `http://localhost:7860`

---

## Как это работает (кратко)

- **LLM**: общение и генерация (резюме/cover/STAR/план/опросник) через `OLLAMA_HOST` с моделью `OLLAMA_MODEL`
- **Embeddings**: `EMB_MODEL` (например, `all-MiniLM-L6-v2`) для векторного сопоставления JD↔резюме
- **PII-анонимизация**: опция скрывает имена/email/телефоны при обработке
- **Граф навыков**: подсказки по хард/софт с рендером PNG
- **Отчёты**: экспорт MD/PDF и Executive Summary

---

## Типовой сценарий

1. Вставьте **JD** и **резюме** или загрузите файлы (PDF/DOCX/TXT/MD)  
2. Нажмите **«Оценить соответствие»** → получите `Job‑Fit`, `Сильные стороны`, `Пробелы`, `Coverage`  
3. **Визуализация**: построить Radar/Heatmap из Coverage  
4. **Генерация**: адаптированное резюме (STAR), сопроводительное, план на 7 дней  
5. **What‑if**: добавьте термины (навыки) и посмотрите Δscore  
6. **ATS‑чекер**: базовая проверка структуры резюме  
7. **Executive Summary**: собрать PDF‑отчёт для рекрутера  
8. **Экспорт**: ZIP/MD/PDF

---

## Траблшутинг

- **`Model not found` в логах app:**  
  Убедитесь, что модель скачана в контейнере Ollama:  
  `docker exec -it ollama ollama pull ${OLLAMA_MODEL}`

- **Ollama не доступен по `http://ollama:11434`:**  
  Проверьте `docker compose ps` и `docker logs ollama`.  
  Убедитесь, что сервис `app` стартует после `ollama` (`depends_on` + `healthcheck`).

- **Порт уже занят (`11434` или `7860`):**  
  Измените публикацию портов в `docker-compose.yml` или `.env` (`PORT=7870`).

- **PII чекбокс не кликается в UI:**  
  В свежем UI стили уже исправлены; если кастомизировали CSS — убедитесь, что у элемента есть `appearance:auto` и `pointer-events:auto` (см. `#stream_out input[type="checkbox"]` в `app.py`).

- **GPU в контейнере:**  
  Установите `nvidia-container-toolkit` и добавьте к `ollama`:
  ```yaml
  deploy: {}
  runtime: nvidia
  environment:
    - NVIDIA_VISIBLE_DEVICES=all
  ```

---

## Полезные команды

```bash
# Стоп и удаление стека
docker compose down

# Обновить образы и пересобрать
docker compose pull
docker compose build --no-cache
docker compose up -d

# Просмотреть логи
docker logs -f skillpilot-app
docker logs -f ollama
```

---

## Лицензия

MIT

---

**Готово!** Открывайте `http://localhost:${PORT:-7860}` и пробуйте на демо JD/резюме.
