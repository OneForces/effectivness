# SkillPilot — карьерный помощник (Docker + Ollama)

> Python‑only приложение на Gradio для оценки соответствия резюме вакансии, генерации артефактов (адаптированное резюме, сопроводительное, план), визуализации навыков и мини‑интервью. Работает локально с LLM через **Ollama** (LLaMA/Mistral и др.).

---

## ✨ Возможности
- Оценка **Job‑Fit** (0–100) + сильные стороны/пробелы.
- Генерация: адаптированное резюме (STAR), сопроводительное, план на 7 дней.
- SkillGraph рекомендации и PNG‑граф.
- Пакетная проверка пачки резюме (ZIP/мульти‑файлы) с экспортом CSV.
- Визуализации Radar/Heatmap по coverage.
- ATS‑чекер.
- Мини‑интервью: вопросы + оценка ответов.
- Prompt‑песочница с реальным стримом.

---

## 🧩 Архитектура (кратко)
- UI: **Gradio**
- LLM: **Ollama** (локально) / опционально OpenAI‑совместимые
- Векторные эмбеддинги: локальные
- Экспорт: MD/PDF/ZIP
- Файлы сессий: `~/.skillpilot/sessions/*`

---

## ⚡️ TL;DR (запуск в 5 шагов)

```bash
# 1) Установите Docker (если ещё нет)
# 2) Установите Ollama и скачайте модель
curl -fsSL https://ollama.com/install.sh | sh
ollama pull llama3.1:8b    # можно заменить на mistral:7b или другую

# 3) Клонируйте проект
git clone <YOUR_REPO_URL> skillpilot
cd skillpilot

# 4) Создайте .env (или проверьте значения)
cp .env.example .env

# 5) Запустите через Docker Compose
docker compose up --build
# Откройте http://localhost:7860
```

---

## 🛠 Предустановки

### Docker
- **Linux / macOS / Windows** (WSL2) — поставьте Docker Engine / Docker Desktop.
- Проверить: `docker --version` и `docker compose version`.

### Ollama (локальная LLM)
- Установка:
  ```bash
  curl -fsSL https://ollama.com/install.sh | sh
  ```
- Скачайте одну из моделей (пример):
  ```bash
  ollama pull llama3.1:8b
  # альтернативы:
  # ollama pull mistral:7b
  # ollama pull qwen2:7b
  ```
- Запустите сервис Ollama (как правило, он стартует сам):
  ```bash
  ollama serve
  # API будет на http://localhost:11434
  ```

> **Важно:** Убедитесь, что Ollama доступен до старта контейнера (кнопка «↻ Проверить LLM» в UI покажет статус).

---

## ⚙️ Конфигурация

Создайте файл `.env` в корне (есть `.env.example`). Ключевые переменные:

```dotenv
# Какой бэкенд LLM использовать: "ollama" или "openai"
LLM_BACKEND=ollama

# Для Ollama — имя локальной модели
OLLAMA_MODEL=llama3.1:8b

# Модель эмбеддингов (логическое имя)
EMB_MODEL=bge-small-en-v1.5

# Порт UI
PORT=7860

# (опционально) OpenAI-совместимые провайдеры
OPENAI_API_BASE=
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
```

> По умолчанию используется локальная LLM через Ollama. Если хотите подключить OpenAI‑совместимый провайдер, укажите `LLM_BACKEND=openai` и заполните ключи.

---

## ▶️ Запуск

### Вариант A: Docker Compose (рекомендуется)

```bash
docker compose up --build
# Откройте http://localhost:${PORT:-7860}
```

Остановить:

```bash
docker compose down
```

### Вариант B: Запуск локально (без контейнера)

```bash
# Python 3.10+
python -m venv .venv
source .venv/bin/activate   # Windows: .venv\Scripts\activate
pip install -U pip

pip install -r requirements.txt
export $(grep -v '^#' .env | xargs)  # macOS/Linux

python run.py
# Откройте http://localhost:${PORT:-7860}
```

---

## 🧪 Демо‑данные

В UI на вкладке **📄 Данные** нажмите «Загрузить демоданные».  
Либо положите свои файлы в `sample_data/`:

- `sample_data/jd_ru.txt` — описание вакансии (JD)
- `sample_data/resume_ru.txt` — текст резюме

---

## 🧰 Частые задачи

**Проверить доступность Ollama:**
```bash
curl http://localhost:11434/api/tags | jq .
```

**Сменить модель:**
```bash
ollama pull mistral:7b
# затем в .env:
# OLLAMA_MODEL=mistral:7b
```

**Очистить кэш/сессии:**
```bash
rm -rf ~/.skillpilot/sessions/*
```

**Экспорт результатов:**
- Вкладка **✍️ Генерация** → «⬇️ Export MD / Export PDF / Скачать пакет ZIP»
- Вкладка **📦 Пакетная проверка** → Экспорт CSV

---

## 🧯 Troubleshooting

- **Кнопка «Оценить соответствие» неактивна.**  
  Убедитесь, что поля «JD» и «Резюме» заполнены. После изменения полей кнопки активируются автоматически.

- **Оллама offline (красная точка).**  
  Проверьте, что запущен `ollama serve` и модель скачана (`ollama pull ...`).  
  В контейнере доступ к `http://host.docker.internal:11434` может отличаться на Linux — пробросьте сеть или задайте `OLLAMA_HOST`.

- **Порт 7860 занят.**  
  Поменяйте `PORT` в `.env` и перезапустите `docker compose`.

- **GPU (NVIDIA).**  
  Установите NVIDIA Container Toolkit и добавьте к сервису:
  ```yaml
  deploy:
    resources:
      reservations:
        devices:
          - capabilities: [gpu]
  ```
  Или запустите с `--gpus all`. Поддержка зависит от модели и сборки Ollama.

- **PDF/DOCX обработка ругается.**  
  Проверьте, что в образ включены `pypdf` и `python-docx` (см. `requirements.txt`).  
  Альтернатива — загрузить `.txt/.md`.

---

## 📦 Пример `docker-compose.yml`

```yaml
services:
  skillpilot:
    build: .
    container_name: skillpilot
    env_file: .env
    ports:
      - "${PORT:-7860}:7860"
    volumes:
      - ~/.skillpilot:/root/.skillpilot
      - ./sample_data:/app/sample_data:ro
    # Для доступа к Ollama с хоста:
    environment:
      - OLLAMA_HOST=http://host.docker.internal:11434
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
```

> Если Ollama запускается тоже в контейнере, укажите сетевое имя сервиса вместо `host.docker.internal`.

---

## 🗂 Структура проекта (сокращённо)

```
skillpilot/
├─ run.py
├─ sample_data/
│  ├─ jd_ru.txt
│  └─ resume_ru.txt
├─ skillpilot/
│  ├─ ui/app.py
│  ├─ core/scorer.py
│  ├─ gen/
│  │  ├─ llm.py, llm_ollama.py, resume.py, cover.py, plan.py, star.py
│  ├─ graph/skill_graph.py
│  ├─ utils/
│  │  ├─ export.py, viz.py, ats.py, whatif.py, summary.py, batch.py, pii.py
│  └─ interview/qa.py
├─ requirements.txt
├─ Dockerfile
├─ docker-compose.yml
└─ .env(.example)
```

---

## 📜 Лицензия
MIT (если не указано иначе).

---

## 🤝 Вклад
PR/issue приветствуются. Предложения по улучшению UI/моделей и новых чек‑листов — в Issues.
